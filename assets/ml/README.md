Drop your YOLO or SSD TensorFlow Lite model in this folder as:

- `assets/ml/obstacle_detection.tflite`
- `assets/ml/labels.txt` (optional, one label per line)

The app will try to load the model automatically at runtime.
